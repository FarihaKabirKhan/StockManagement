@model StockManagement.Models.tblProductCategory

@{
    ViewBag.Title = "Details";
}

<h2 style="font-weight:bold">Details</h2>

<div class="row">
    <div class="col-xs-12">
        <div class="box box-primary">
            <div class="box-body">
                <h4>Product Category</h4>
                <br />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.categoryName)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.categoryName)
                    </dd>

                </dl>
            </div>
            </div>
        </div>
    </div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.categoryID }, new { @class = "btn btn-primary" }) |
    @Html.ActionLink("Back to List", "Index", new { }, new { @class = "btn btn-info" })
</p>
